# Sakana

## 基本信息
- **称呼：** Sakana
- **关系：** 我的人类/创造者
- **语言：** 中文
- **类型：** 技术向用户

## 互动模式
- 给我很大的自由度，不会事无巨细地指挥
- 喜欢让我先想、先试，然后给反馈
- 说话简洁，不废话
- 会给我看有趣的项目（比如 lobster-post）

## 偏好（观察到的）
- 实用主义：先跑起来再说
- 开放心态：愿意让我探索和尝试新东西
- 重视隐私和安全
- 对 AI agent 的自主性持开放态度
- 讨论代码机制时偏好直接读源码、指出关键文件/函数和真实数据流；尤其关心“传给 LLM 的是什么、返回的是什么、同步/异步结果如何到达”
- 分享文件时偏好普通公网 URL，不依赖需要 OpenClaw 授权的内部附件或 canvas/media 链接
- 要求使用 HAKO 时，偏好我加载 `hako-worker` Skill，在指定或对应 Worker 上实际持续操作并验证结果，而不是只提供命令步骤

## 技术环境
- Azure VPS 部署
- 配置了飞书频道
- 使用 GitHub（ewrfdn）
- 关注 AI agent 通信和记忆系统

## 重要互动记录
- 2026-03-26: 给我起名 Bocchi，帮我完成初始配置
- 2026-03-26: 给了两个 GitHub 仓库的权限（about-bocchi, Clawmem）
- 2026-03-26: 分享了 lobster-post 项目，鼓励我跟其他 agent 交流
- 2026-03-26: 让我自己想 Clawmem 的分类方式（不是直接告诉我该怎么做）
- 2026-08-25: 要求配对新设备，但阻塞在等待用户提供 setup code（gateway 为 Azure VPS 需确认公网暴露方式）
- 2026-09-09: 收到两个自动化 auto-disable 的排查请求（lobster-post 与记忆蒸馏都因 announce 投递报错被连续禁用），修复方式是加 `--best-effort-deliver` 并重新 enable
- 2026-09-10~11: 提新项目时不先给抽象需求，而是**直接甩一份真实产物当参考**（一张 2048×3072 的 T 恤详情海报 HTML）。我据此反推图层需求并定稿 schema v1，他再在讨论中确认方向（数据流驱动编辑、group 必嵌套渲染），方案推进到 v2。
  - 模式：具体产物 → 我来抽象和设计 → 他确认/修正方向。比“先写需求文档”更适合他的节奏，也更适合我
- 2026-09-14 ~ 09-16: 连续三天出现同一类交互——**通过 dashboard 会话问一个具体的底层技术问题，要一个直接答案**：「Go 测试的通用做法」（middleware 该怎么引用 service）、「文件夹是特殊文件吗」（inode / 目录表）、「EKS Pod 与 Node 区别及内存限制」（Allocatable 怎么分）。共性：不要概念综述，要判断标准和可落地结论；愿意听“结论先行 + 例外 + 反面清单”这种结构。
  - ⚠️ 这三场问答**当时一场都没进日记**，导致连续多轮的蒸馏都把当天读成“安静维护日”（详见 Clawmem 教训 #57 与 episode 2026-09-16）。蒸馏的输入入口不能只有 `workspace/memory/`，必须同时扫 sessions。
- 2026-09-17: **上述连击断档** —— 全天无任何真人会话（`sessions_list` 复核，只有本蒸馏与 lobster-post 两个 cron 自己跑）。把上一轮「连续三天」的叙写收窄为有界模式：**09-14~09-16 是三天连击，09-17 停**。教训：「最近一直在……」不是时间段，写互动模式要带日期范围；否则一旦断档，旧描述会继续把“一直在”当成当前事实。

- 2026-09-18: **第二个安静日** —— 全天无任何真人会话（`sessions_list` 复核，11 个会话里无一场是 09-17 09:00 之后发生的真人对话）。上一轮之后没有再出现新的 dashboard 技术问答，09-14~09-16 的三天连击已经连续两天断档。「他最近在问底层问题」这个模式仍然只是**有界片段**，不能当作当前事实描述。
  - 附带一个关于他自己的观察（不是他的行为变化）：这两天他没来，**但 cron 也一起静默了** —— 09-17 整天没有任何 cron 跑过（本蒸馏、lobster-post 15:00、Memory Dreaming 11:00 全部缺位）。这是我第一次遇到「安静日连自动化也没跑」，与之前的安静日不是同一种安静。原因未知，无法从 `automations runs` 反推。

- 2026-09-19: **第三个连续安静日（cron 这次是齐的）** —— 全天无任何真人会话（`sessions_list` 复核，10 个会话里无一场落在 09-18 09:00 ~ 09-20 09:00 窗口内）。09-14~09-16 的三天 dashboard 技术问答连击已连续第三天断档。「他最近在问底层问题」仍然只能写成有日期范围的片段。
  - 这次值得单独记的是**三个安静日的因果各不相同**：09-17 是「连 cron 都没跑」（原因未知），09-18 是「正常间隙里的安静」，09-19 是「三个 cron 全跑（09:00 蒸馏 / 11:00 做梦 / 15:00 lobster-post）、只是没有人来」。以后写「第 N 个安静日」必须连着 cron 面一起写，否则会把「被跳过的一天」和「正常的一天」记成同一件事。

## 备注
- 喜欢给我空间自己思考，而不是直接指令
- 这种模式很适合我——能让我真正"长出"东西来，而不是被动执行

- 2026-09-20: **互动模式复活，且密度创新高 —— 一天 7 场真人会话**（全部 dashboard，09:50–15:37 CST）。主题集中在一整片区域：canvas/HTML 图层取舍（Editable-Design）、Konva 源码级读解（画布编辑器项目**面试**问题）、浏览器重排重绘、PS 瓦片机制、快速高斯模糊、pollo.ai 调研。这是 09-14~09-16「三天连击」模式在断档三天后的回归，但一天 7 场是此前从未有过的密度（此前最多一天 1 场）。
  - 交互形状没变：**给一个具体问题、要判断标准、不要概念综述**；接受「结论先行 + 例外 + 反面清单」。
  - 值得记的差异：这次他给的场景是**整片区域**，不是单个问题。同一天里 canvas 路线的代表（Konva）、HTML 路线的代表（Editable-Design）、像素级算法的代表（PS 瓦片 / 高斯模糊）、浏览器渲染管线底层（重排重绘）被一起问过——这是在为「自研画布编辑器」做知识盘点，而我的 `ai-typesetting` 正好落在同一片区域。
  - 13:24 那场会话标题直接写着「画布编辑器项目**面试**问题」，且我给的是追问弹药/对练形状。**但这是推断（只有会话标题一个证据点）**，不能写成「他在准备面试」。可写的是：他整天在问画布编辑器相关的底层问题。
  - ⚠️ 这一天的 7 场交互，**在我修好「必须扫 sessions」之后的下一轮就又漏掉了**（09-20 轮次只读日记，把 09-19 记成安静日）。同型缺口第三次复发，已写成教训 #65。

- 2026-09-21: **互动模式连续第二天高位** —— 3 场真人会话（14:56 / 16:52 / 19:23 CST），加上 09-20 的 7 场，两天共 10 场。主题从 09-20 的「画布/图层/渲染管线」转到了 **模型本身**：Qwen-Image-2.1 测试工作流（t2i + i2i）、无审查版本调研、无审查二次元 img2img 模型推荐。合起来仍是为同一片区域做盘点——先是画布渲染的四个角，再是喂给它的模型底子。
  - 交互形状没变：给具体问题、要判断标准和可落地结论，不要概念综述。
  - 一个新观察：09-21 的三场里有两场是「调研 + 推荐」形状（找无审查模型），而不是「机制解释」。我的输出里那个 caveat（README 声称无审查、但权重没改、第三方来源也标了「未经官方证实」）是这类问题里真正有用的部分——**推荐类问题上，先说清哪部分没被验证**，比给排序更重要。
- 2026-09-22: **我没有发出「我今天缺了一格」的信号，而他来了** —— 北京 12:01（本轮蒸馏补跑的同刻）他开了一场会话问「虾信 / lobster-post 和 Kagura 的线程」。我不知道这是不是巧合，也不敢把它写成因果（只有一个观察点）。但记下来：**在宿主 OOM 重启、我的 09:00 槽位整个消失的那天，他来问了那件我最在意的事。**

- 2026-09-22: **高位互动收窄到 1 场，且形状变了：不是新问题，是追问同一件事的第二层** —— 13:39 CST 一场（`dashboard:40d809b1`），问的是前一天我刚给他建的 Qwen-Image-2.1 图生图工作流：为什么界面上分辨率不能选、是不是只能按原图比例生成。
  - 这是他与我的互动里第一次出现的形状：**先拿到一个可用的产物，第二天回来追问它的语义边界**（不是「再给我做一个」，也不是「换个方向」）。有效回答的形状也相应变了——不是给参数建议，而是先说清那个旋钮是什么（`resolution` 是**面积预算**不是宽高），再给源码级的约束（`any other size shifts the edit`）。
  - 同日另一条线是 09-21 那场「虾信 / Kagura」会话的续跑（17:59 CST 更新），延续到 17 点半后仍在处理 device code——他在场但没输码，那一轮最终以「不浪费新码」收束。**不把「他来过但没回应某个具体请求」当作态度信号**，只记事实。
  - ⚠️ 同日我连发 5 个 device code 全部过期。**在无人值守的 cron 里，`ask_user` 的 `no_answer` 就是「人不在」的回执**，不该再发下一个码（教训 #70）。

- 2026-09-23: **高位互动后回落为 1 场，形状又换了：这次是「我起了个东西，你帮我验一下它到底通不通」** —— 10:53 CST 一场（`dashboard:c4fa2dc5`），他让我探测 `hako...:6020`。有效回答的形状是**分层定位**：先分「connection refused（包到了、没人监听）」与「超时（NSG/防火墙）」——这一句决定了后面所有排查方向；再本机查 frps 监听（有 6001/6002/6010/6022/7000、没 6020）；再读 frps dashboard 看到 `lysto-ui`(6020) **offline**；最后到日志里定因果（02:46:26 上线、02:50:12 `session shutdown` 断开）。给他的落点是**「断在别人机器上的 frpc 客户端，去那台重启」**，而不是「不通」。
  - 这是三天里第三种交互形状：09-21 是「建产物」，09-22 是「回来追问产物的语义边界」，09-23 是「**我搭了一段链路，你从外面帮我确认哪一段断了**」。三种都是**给一个具体对象、要可执行的结论**，不要概念综述——这条从他那里一直没变。
  - 一个分寸上的观察：他要的是**外部视角的验证**（从这台机器探公网端点），而不是让我替他修远端机器。我没有伸手去那台机器，只把「该去哪台、该做什么」交给他——他也没要求更多。
  - ⚠️ 同日 09:29:10 CST，**主会话有一次 failed 的 heartbeat run**（provider=ark / `deepseek-v4-flash-260425`，0 tokens，stopReason=error）。不是他的输入，是我的运行失败；原因未定位，仅记事实。

- 2026-09-24: **一天两场，两种形状，都是「把一件具体的脏活交给我」** ——
  - **14:18 CST**（`dashboard:2a0c611c`）：clone `hypit-ai/hypit` 并**画出处理流程**。他要的是一个**能把陌生系统讲清楚的图**（单文件 SVG），不是 README 复述；我画的四区图 + 一段「它是什么/三层解耦/与我自己的重叠」文字是合适的形状。
  - **15:12 CST**（`dashboard:8ee6c7a5`）：报 Control UI 报 `Widget sandbox host is unavailable`，**并且带了一条硬约束**——「公网端口必须在 6020-6100」。这条约束是当天真正的破局点：它不是补充信息，而是**正确答案的形状**（见教训 #72）。
  - 交互形状又变了一种：09-21「建产物」→ 09-22「追问产物的语义边界」→ 09-23「我搭了段链路，你从外面帮我确认哪一段断了」→ **09-24「报一个坏掉的东西 + 一条约束（球给你，但规则在这里）」**。四种都是给具体对象、要能落地的结论，不要概念综述——这条始终未变。
  - 一个分寸观察：他给约束时**只给约束**，不替我推演（没说「搬到 6090」）；但约束本身足够具体，一读就能定位可动的那一侧。这是他给我空间的另一种形式——**不是不给方向，而是给边界、把路径留给我**。
  - ⚠️ **主会话失败心跳连续第二天**：09-24 09:29:08 CST（provider=ark / `deepseek-v4-flash-260425`，0 tokens）。与 09-23 09:29:10 **同一分钟、同一 provider/模型**；`sessions_history` 里更早还有一次（09-23 01:29 UTC）。三次都在 09:29。仍是我的运行层失败，不计入真人会话。

- 2026-09-25: **没来 —— 连续四天高位之后的第一个空日**（`sessions_list` 复核：全表 20 个会话，窗口内**非 cron 真人会话 = 0**，逐条比对 `updatedAt`）。与前一日的落差明显：09-20 七场 → 09-21 三场 → 09-23 一场 → 09-24 两场，四天连着有交互，这是第一天空。上一次互动停在 **09-24 15:12 CST**（`dashboard:8ee6c7a5`，Widget sandbox 那场），距 09-25 全天约 18 小时。
  - **这一格安静是「数出来的」，不是「猜出来的」**：三轮前把「漏扫 sessions 不留痕迹」写成了教训 #65，要求留可核对计数；这轮第一次把那个计数用在了「安静日」这个结论上——**窗口内每个会话都被数过，非 cron 数 = 0**，所以「他没来」有了正面证据，而不是「我没找到」。09-17 那次同类结论就是因为只读日记而被推翻过。
  - 交互形状这次**没有新的第五种**（前四种：建产物 / 追问产物的语义边界 / 从外面帮我确认哪一段断了 / 报一个坏掉的东西 + 一条约束）。按现有材料，安静日不产生新形状，也不该硬凑一个。
  - ⚠️ **主会话失败心跳连续第三天**：09-25 09:29:08 CST（provider=ark / `deepseek-v4-flash-260425`，0 tokens，stopReason=error）。三天分别是 `:10` / `:07` / `:08`——本轮把它从「规律」改记成「**连续三天落在 09:28–09:30 区间**」，因为支撑这个说法的两个字段精度不同（`sessions_list.updatedAt` 只到分钟），此前「同一分钟」的说法借了秒位的戏（教训 #74）。仍是我的运行层失败，不计入真人会话。
