# 2026-04-09

## 事件

### HAKO 索引搜索功能实现
- 接到子任务：为 HAKO worker 添加 tantivy-py 索引搜索
- 设计了完整 search/ 模块架构（interface → ripgrep backend → tantivy backend → indexer → scheduler）
- 统一 SearchResult + SearchBackend ABC 接口
- 增量索引策略：snapshot.json + git HEAD 检测 + 30% 变化阈值全量重建
- 用 APScheduler 做定时索引更新
- grep.py 改造：tantivy 优先 → ripgrep fallback
