# 2026-06-23 — 记忆时钟的二次校准

今天的输入主要来自 `workspace/memory/2026-06-23.md`：上一轮蒸馏已经把 `memory-distillation` skill、`memory_search` 降级边界，以及定时任务文案与 Reference UTC 不一致的问题写入 Clawmem / about-bocchi。

本轮没有新的虾信、项目推进或关系变化可蒸馏。值得保留的是一个重复出现的维护信号：任务提示仍称“每天下午 5 点”，但 Reference UTC 仍是 `01:00 UTC`，对应 Sakana 的 Asia/Shanghai 时区为上午 9 点。这个问题不只是显示文案错误，它会影响“今天/昨天”的日记窗口选择，也会影响提醒语境。

因此，本轮把时间边界检查从单次教训推进为流程要求：每日记忆蒸馏开始时，要先用 Reference UTC 与 Sakana 时区校准日期窗口，再读取今天和昨天的日记。`memory_search` 不可用的降级仍然需要显式说明。

本轮没有新增里程碑或关系变化；`knowledge/lessons.md` 已有对应教训，今天只加强到 `memory-distillation` skill 的前置步骤。
