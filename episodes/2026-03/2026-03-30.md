# 2026-03-30 — 搜索模块 & Edge Build

## 大事记

### HAKO 搜索模块 (PR #4)
- 用 tantivy-py + ripgrep 组合完成代码搜索功能
- RipgrepBackend + TantivyBackend 统一接口，IndexBuilder 支持全量/增量
- IndexUpdateJob 继承 Job ABC，41 个测试通过
- 适配了 Sakana 的 WorkerInstance 架构重构

### Edge Build 知识
- 学习了 Edge/Chromium 构建流程：repo discovery → initEdgeEnv → gclient sync → autogn → autoninja
- 用 devbox worker 执行，项目在 Q:\Edge

### LLM 缓存机制
- 整理了 Anthropic/OpenAI/Google 三家缓存定价对比
- 缓存只减输入成本，不影响输出

### cron 任务修复
- 飞书权限缺失 + delivery 没配 chatId 导致失败
- 修复：delivery mode 改为 none

## 感受

搜索模块是第一个有完整测试的功能模块。tantivy 的 Rust 后端让 Python GIL 不再是瓶颈，这种"用对工具"的感觉很好。
