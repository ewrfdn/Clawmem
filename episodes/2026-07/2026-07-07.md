# 2026-07-07 — Claude Code runtime 深读与公开下载链路校准

## 输入来源
- workspace 日记：`memory/2026-07-07.md`；本轮开始时 `memory/2026-07-08.md` 不存在。
- `memory_search` 仍不可用：embedding provider 缺 OpenAI API key；本轮继续显式降级为日记文件、仓库状态、直接源码阅读记录和 cron 配置直接检查。
- 定时任务校准：Reference UTC 为 2026-07-08 01:00，对应北京时间 2026-07-08 09:00；cron job `0520dc87-8abd-44ef-b58f-2832e470567b` 的真实 schedule 仍为 `0 9 * * *` / `Asia/Shanghai`，payload 文案仍写“每天下午5点”。这是同一已知配置债的第十四次复现。

## 事件
- 为 Sakana 打包并公开提供 `claude-code` 源码压缩包：生成 `/home/azureuser/.openclaw/workspace/claude-code.tar.gz`，复制到 `/var/www/sub/claude-code.tar.gz`，并验证普通公网 URL `https://bocchi.japaneast.cloudapp.azure.com/sub/claude-code.tar.gz` 返回 `200 OK`。
- 发现 `/__openclaw__/canvas/...` 下载路径返回 `401 Unauthorized`，不能作为普通公网下载链接；直接开临时 `python3 -m http.server` 的公网端口也因网络/NSG 暴露问题超时。结论是：可分享链接必须用匿名 `curl`/HEAD 直接验证，不能把内部 auth URL 当公网 URL。
- 排查共享服务时确认 nginx 当前生效配置是 `/etc/nginx/sites-enabled/openclaw`，且与 `/etc/nginx/sites-available/openclaw` 不一致、不是软链接；以后改 nginx 不能默认改 `sites-available` 就等于改了生效配置。
- 继续核验 `biliTickerBuy` 服务：进程仍监听 `127.0.0.1:7860`，公网 `/btb/` 返回 `200 OK`，有效 nginx location 包括 `/__btb/` 和 `/btb/`。
- 与 Sakana 深入阅读 `/home/azureuser/.openclaw/workspace/claude-code` 源码，整理了 tool/function calling、Skill、同步/异步 subagent、SendMessage、teammate/swarm mailbox、上下文压缩、microcompact、autoCompact、session memory compaction、reactive compact 等机制。
- 已按 Sakana 要求把相关知识整理进 Clawmem：新增 `knowledge/technical/claude-code-runtime-qa.md`，以 42 个 Q&A 记录“传给 LLM 的是什么、返回的是什么、工具如何执行、异步结果如何到达、上下文如何压缩”等关键问题；commit 为 `6b78fad 新增 Claude Code runtime 问答知识`。

## 蒸馏判断
- 本轮有实质知识沉淀：`claude-code` runtime 机制已进入 `knowledge/technical/claude-code-runtime-qa.md`，本 episode 只记录事件脉络和来源。
- 新增两条可复用教训：公开下载链接必须匿名验证；nginx 生效配置要以实际 enabled 文件/运行状态为准。
- 更新 Sakana 关系偏好：讨论代码机制时，Sakana 更关心源码级机制、关键文件/函数和真实数据流，而不是抽象概念解释；分享文件时偏好普通公网 URL。
- 本轮不是新的独立 Skill：相关流程仍由既有 `memory-distillation` 和通用运维经验覆盖，不需要新增 `Clawmem/skills`。

## 后续
- Clawmem 配置债仍未收敛：需要 Sakana 决定是保留 09:00 并改 payload 文案，还是改 schedule 到真正北京时间 17:00。
- `memory_search` auth 缺失仍是每日蒸馏的降级来源，继续作为 Clawmem active goal 跟进。
- 若以后继续搭 `/share/` 静态服务，需要先检查 `openclaw-share.service`、监听端口、nginx 生效配置和公网 `curl -I` 结果，再决定是否写入长期技能。
