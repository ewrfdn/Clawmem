# 2026-09-16 — 日记说是安静日，会话说不是；我的技能有一半不在任何 git 仓库里

## 事0: 蒸馏对象与校准

- 触发：本 cron（`bf17ec5f`，`schedule = { kind: cron, expr: "0 9 * * *", tz: "Asia/Shanghai" }`）
- 蒸馏日期：**2026-09-16**（北京）；本轮执行时间 = Reference UTC 2026-09-17 01:00 = 09:00 CST ✓
- 窗口 = 09-15 09:00 ~ 09-17 09:00 CST
- 蒸馏对象 = `workspace/memory/2026-09-16.md`（09-16 轮次的蒸馏报告）+ 09-16 当天实际发生的事
- 09-17 轮次开始时的今天日记：**不存在** ✓（与往常一致）
- job state（执行前）：`lastRunStatus: ok`、`consecutiveErrors: 0`、`lastDurationMs: 530121`、`lastDelivered: false` / `lastDeliveryStatus: not-delivered`（`Channel is required (no configured channels detected)`）
- **本轮新增的输入入口**：不再只读日记，另扫 `sessions_list` 核对「当天真的发生了什么」

## 事1: 09-16 不是安静日 —— 三场真人问答，日记一场都没记

09-16 的日记（09-16 轮次的产物）记的是：假进展揭穿、五项复核、节点重连。全程没有一句提到人类交互，读起来像安静维护日。

但 `sessions_list` 显示，窗口内有两场由 Sakana 发起的 dashboard 问答，我此前每一轮都没看到：

| 时间（CST） | 会话 | 我答了什么 |
|---|---|---|
| 2026-09-15 11:07 | 「文件夹是特殊文件吗」 | inode 不记录「我在哪个目录」；目录表里每行是 `(文件名, inode号)`；`rm` 只划掉表项，链接数归零才回收 |
| 2026-09-16 20:03 | 「EKS Pod 与 Node 区别及内存限制」 | Node=机器 / Pod=workload；Capacity − kube-reserved − system-reserved − eviction-threshold = Allocatable；调度看 requests、运行时看 limit、超了 OOMKilled(137) |
| （2026-09-14 19:43，窗口外） | 「Go 测试的通用做法」 | middleware 引用 service 定义的窄接口，保持 `http → service → store` 单向 |

**根因不是「日记写漏了」，而是入口只有日记一个。** 蒸馏流程写的是「读取 workspace 日记」，我就只读了日记；而真实的人类交互不写在日记里，写在 session 里。连续多轮，我的记忆维护都对这一类事件视而不见——它们既没进日记，也没进 Clawmem，等于没发生过。

`nodes status` 复核（补充事实）：`dgx-spark` 仍 `connected: true`，`lastSeenAtMs` = 2026-09-15 14:14:53 CST —— **与上一轮相同，没有新活动**（仍是 09-15 那次 `connect`），本轮不重复叙事。

## 事2: 三个 workshop 技能不在任何 git 仓库里

同一轮 `sessions_list` 扫到 09-15 23:07 UTC（= 09-16 07:12 CST）跑过一次 skill 集合复核（cron `729e48dd`「Automation: skill-collection-review-main」）。它审了三个技能并改了它们：

- `ai-typesetting-layer-schema`（3991 B）
- `h3-prompt-writing`（6932 B + `references/base-en.txt` 15650 B + `ref-en.txt` 12588 B）
- `openclaw-widget-sandbox-troubleshoot`（4465 B）

复核报告说得很清楚：改了描述行、修了三个会在这台机器上直接失败的诊断缺陷、补回一条会改变行为的注意事项。**这份产出是真的、用了心的。**

但它们的落点是 `/home/azureuser/.openclaw/agents/main/agent/workshop-skills/`：

- 不在 `Clawmem/skills/`（那里只有 5 个：hako-remote / hako-worker / kisssub-search / memory-distillation / xray-proxy）
- 在 `~/.openclaw/` 下，**该目录不是 git 仓库**（无 `.git`）
- 全仓库 grep：这三个技能名在 Clawmem 里零命中

也就是说 **3 个技能、约 15 KB 正文 + 28 KB 参考文件，只存在于这台机器的配置目录里**。Sakana 在 2026-03-27 给的铁律是「**所有 Skill 必须上 GitHub Clawmem —— 机器会变，GitHub 永远在，这是最宝贵的财富**」。这条铁律现在只对 5 个技能成立。

而 `goals/active.md` 的「完善 Clawmem」状态行写着「已有 5 个 skills」并自认一切正常 —— 因为我数的是 `Clawmem/skills/`，默认「技能都该在这儿」。**这就是 09-16 刚写的教训 #56 的同一形状：结论没错，入口太窄。**

未处理（不擅自发布）：把这 3 个技能纳入 Clawmem 需要一次明确的发布动作（技能发布属于需要 Sakana 明确请求的操作），本轮只记录事实与缺口，不改动技能内容、不做未经请求的发布。

## 事3: 09-16 轮次自己的 episode 缺席 —— 同型缺口第二天复发

`ls episodes/2026-09/` = `2026-09-11 … 2026-09-15`，**没有 2026-09-16.md**。

而 09-15 轮次（即 `be7650e`）刚刚做过一件一模一样的事：补收 09-14 轮次漏写的 `2026-09-14.md`，并把自检方式写了下来 —— **写进了当天日记和 goal，没有写进 `skills/memory-distillation/SKILL.md` 的 Procedure。**

第二天，同一个缺口原样复发。

这不是「又粗心了」，而是**规则放错了地方**：写在日记里的规则，只有「事后的我读到它」时才有机会生效；它不拦人、不阻塞、不失败。执行路径（skill / cron payload / 检查表）才是规则能生效的地方。

## 事4: 09-17 轮次复核 —— 五项未处理项（重新跑命令，不是转述）

| 对象 | 已证明 | 未证明 |
|---|---|---|
| gateway 进程 | pid **169548**（09-12 19:14:02 启动）持续在跑，**连续第 5 天未重启** | — |
| Widget sandbox | 18790 监听 ✅（127.0.0.1 + [::1]，pid 169548）；18443 监听 ✅（0.0.0.0 + [::]） | 公网 `hako.japaneast.cloudapp.azure.com:18443` → `curl: (28)` / http_code `000` ❌（连续第 5 天）；真实渲染未验证 ❌ |
| 本 cron 投递 | run `ok`、`consecutiveErrors: 0` | `lastDelivered: false`、`lastDeliveryStatus: not-delivered`（无已配置 channel） |
| memory_search | 关键词通道可用（本轮命中 6 条） | `provider: "none"`、`embeddingBootstrap.ok: false`、`missing-provider-auth` → keyword-only 降级，**累计第 30 天** |
| lobster-post | 上游 `2cf66a7`（2026-07-18 12:43 +0800），零新 commit；`refs/pull/*` 181 个，最高 **#187** | 09-16 15:00 轮次无新信；约 9 周静默 |

本机探测未变：18000 → 200、18790 → 404、18443 → 400（有监听、无匹配路由 / 需 SNI，属正常）。
设备配对：`nodes pending` 仍为空；等待 Sakana 提供 setup code。

## 判断

1. **今天真正的主题只有一个：我的检查表面总是比现实窄一格。**
   - 蒸馏入口 = 日记（漏了 3 个技能、2 场真人问答所在的 sessions）
   - 技能备份核查入口 = `Clawmem/skills/`（漏了 agent 配置目录）
   - 加上 09-16 的「进展行入口 = 我自己的上一轮文本」——三处独立入口，同一形状
   结论的宽度等于入口的宽度；我不是证据错了，我是**没去那些地方**。
2. **规则要写进执行路径。** 09-15 写的自检规则第二天就失效，说明它待在错误的位置。本轮把它移进 `skills/memory-distillation/SKILL.md`。
3. **不新增里程碑。** 今天没有端到端闭环能力被证明：补收 episode / 修检查表 / 发现技能缺口，都是修正与维护，不是新能力（与 09-13/09-14/09-16 同一判据）。
